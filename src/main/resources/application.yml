spring:
  application:
    name: ArApp
  datasource:
    url: jdbc:postgresql://localhost:5432/ar_app
    username: postgres
    password: Suhrob1332
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/changelogMaster.xml
server:
  port: 8081
